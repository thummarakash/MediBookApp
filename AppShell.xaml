<?xml version="1.0" encoding="UTF-8" ?>
<Shell x:Class="MediBook.AppShell"
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:MediBook.Pages"
       Shell.FlyoutBehavior="Disabled"
       Shell.NavBarIsVisible="False"
       BackgroundColor="{StaticResource PrimaryNavy}"
       TitleColor="White">

    <ShellContent Route="splash" ContentTemplate="{DataTemplate pages:SplashPage}" />
    <ShellContent Route="login" ContentTemplate="{DataTemplate pages:LoginPage}" />
    <ShellContent Route="register" ContentTemplate="{DataTemplate pages:RegisterPage}" />

    <TabBar Route="main"
            Shell.TabBarBackgroundColor="White"
            Shell.TabBarTitleColor="{StaticResource PrimaryBlue}"
            Shell.TabBarUnselectedColor="{StaticResource MutedText}">
        <ShellContent Title="Home" Route="home" ContentTemplate="{DataTemplate pages:HomePage}" />
        <ShellContent Title="Doctors" Route="doctors" ContentTemplate="{DataTemplate pages:DoctorsPage}" />
        <ShellContent Title="Appointments" Route="appointments" ContentTemplate="{DataTemplate pages:AppointmentsPage}" />
        <ShellContent Title="Documents" Route="documents" ContentTemplate="{DataTemplate pages:DocumentsPage}" />
        <ShellContent Title="Profile" Route="profile" ContentTemplate="{DataTemplate pages:ProfilePage}" />
    </TabBar>
</Shell>
